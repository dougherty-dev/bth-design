{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main" role="main">
        <div class="tech-page">
            <div class="grid-sidebar">
                <div>
                    <h2 class="center">tech</h2>
                </div>
                {% for page in pages("technology") %}
                <div>
                    <h3><img src="{{ assets_url }}/svg/{{ page.title | lower }}.svg" class="tech" alt="{{ page.title }}">
                    <a href="{{ page.url }}">{{ page.title }}</a></h3>
                </div>
 {% endfor %}
            </div>
            <div class="grid-tech-content">
                {{ content }}
            </div>
        </div>
    </div>
    
    {% include 'incl/footer.twig' %}
</body>

</html>
